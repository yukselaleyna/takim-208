import 'package:flutter/material.dart';

void main() => runApp(const SavedPage());

class SavedPage extends StatelessWidget {
  const SavedPage({Key? key})
      : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child:
      Text("(Saved)"),
    );
  }
}